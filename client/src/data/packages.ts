import { Package } from '../types';

// This file now serves as a fallback/example structure
// Packages are fetched from Tebex API at runtime
// You can keep example packages here for local development if needed

export const packages: Package[] = [
  // Packages are now loaded from Tebex API
  // See App.tsx and tebexService.fetchPackages()
];
